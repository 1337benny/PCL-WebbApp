// Please see documentation at https://learn.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

    //    document.addEventListener("DOMContentLoaded", () => {
    //        // Första formuläret (Checklistans namn)
    //        const checklistNameInput = document.querySelector("input[asp-for='Name']");
    //const checklistSubmitButton = document.getElementById("hideButtonChecklist");

    //// Andra formuläret (Uppgiftens titel)
    //const taskNameInput = document.querySelector("input[name='taskName']");
    //const taskSubmitButton = document.getElementById("hideButtonTask");

    //        // Funktion för att hantera knappens tillstånd
    //        const toggleButtonState = (input, button) => {
    //    button.disabled = !input.value.trim(); // Inaktivera om fältet är tomt
    //        };

    //        // Eventlyssnare för checklistans namn
    //        checklistNameInput.addEventListener("input", () => {
    //    toggleButtonState(checklistNameInput, checklistSubmitButton);
    //        });

    //        // Eventlyssnare för uppgiftens titel
    //        taskNameInput.addEventListener("input", () => {
    //    toggleButtonState(taskNameInput, taskSubmitButton);
    //        });

    //// Initiera tillstånd vid sidladdning
    //toggleButtonState(checklistNameInput, checklistSubmitButton);
    //toggleButtonState(taskNameInput, taskSubmitButton);
    //    });
